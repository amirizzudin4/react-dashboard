import React from "react";
import { Button } from "boros";

function LoginPage() {
    return (
        <React.Fragment>
            <p>this is page 1</p>
            <Button variant="contained" color="primary">
                login
            </Button>
        </React.Fragment>
    );
}

export default LoginPage;
